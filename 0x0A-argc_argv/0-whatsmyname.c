#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * Description: program to print  the file name
 * @argc: parameter
 * @argv: parameter
 * Return: 0 success
*/
int main(int __attribute__((unused)) argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
